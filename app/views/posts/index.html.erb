<div class='post-index'>
<h1>All posts</h1>

<div>
    <% @posts.each do |post| %> 
        <h2><%= link_to post.title, user_post_path(user_id: post.author.id, id: post.id) %></h2>
        <p><%= post.text %></p>
    <% end %>
</div>
</div>
